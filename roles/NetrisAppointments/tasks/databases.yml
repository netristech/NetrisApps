---
- name: Copy database files
  copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
  with_items:
  - { src: 'emailqueue.sql.xz', dest: '/home/netris/' }
  - { src: 'netriscal.sql.xz', dest: '/home/netris/' }
  
- name: Extract database files
  command: "xz -d {{ item }}"
  with_items:
  - /home/netris/netriscal.sql.xz
  - /home/netris/emailqueue.sql.xz
  
- name: Install databases
  mysql_db:
    login_user: root
    login_password: "{{ na_root_pw }}"
    name: "{{ item.name }}"
    target: "{{ item.src }}"
    state: "{{ item.state }}"
  with_items:
  - { name: '{{ eq_database }}', src: '/home/netris/emailqueue.sql', state: 'import' }
  - { name: '{{ na_database }}', src: '/home/netris/netriscal.sql', state: 'import' }
  
- name: Remove files
  command: "rm -f {{ item }}"
  with_items:
  - /home/netris/emailqueue.sql
  - /home/netris/netriscal.sql
